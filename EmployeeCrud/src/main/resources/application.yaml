spring:
  application:
    name: EmployeeCrud

  datasource:
    url: jdbc:mysql://localhost:3306/employee_db
    username: root
    password: '4245'

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect